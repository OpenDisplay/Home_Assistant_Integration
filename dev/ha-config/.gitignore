# Everything here is generated by a running Home Assistant instance
# (onboarding state, .storage/, logs, tts cache, ...) — instance-specific,
# never checked in. Only the hand-written configuration.yaml is tracked.
*
!.gitignore
!configuration.yaml
